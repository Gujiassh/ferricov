# M1 / v0.1 Tracefile Core — Support Matrix

Status: **ACTIVE** (controller-approved exclusions)  
Date: 2026-08-18  
Owner: main-controller  
Source tip at decision base (pre-GO tip): `331447f7d91b926e8b3536f8fca10a3c6b492ec2`  
Linked go/no-go: [`m0-go-no-go.md`](m0-go-no-go.md)  
Linked model scope: [`reviews/m0-model-blocker-scope.md`](reviews/m0-model-blocker-scope.md)  
Linked residual N/A: [`reviews/m0-residual-s5-signed-na.md`](reviews/m0-residual-s5-signed-na.md)  
Linked diagnostics closeout: [`reviews/m0-diagnostics-wave3-review.md`](reviews/m0-diagnostics-wave3-review.md)  
Agent handoff: [`m1-tracefile-core-agent-spec.md`](m1-tracefile-core-agent-spec.md)

## Purpose

Authorize a **bounded** M1 Tracefile Core start without hollow-closing M0
residuals, inventing product evidence, or pretending model fuzz/limit work is
done.

This matrix is the explicit exclusion record required by the M0 go/no-go and by
`m1-tracefile-core-agent-spec.md` (“resolved **or** explicitly excluded”).

## Milestone shape

| Item | Decision |
| --- | --- |
| Milestone | M1 / v0.1 Tracefile Core preview |
| Product claim | **none** — `product_compatibility_evidence` stays **false** until M1-CORE-010 parity evidence |
| Perl / Oracle copy | **forbidden** — Rust-native model; no Perl object-layout copy; no relaxed Oracle differential |
| Allowed crates | `crates/model`, `crates/tracefile` (+ focused tests/fixtures under controller approval) |
| Forbidden crates (this milestone) | `crates/cli`, `crates/ops`, `crates/report`, install/packaging, capture orchestration |

## In scope (authorized now)

| ID | Work | Notes |
| --- | --- | --- |
| `M1-CORE-001` … `M1-CORE-008` | Model primitives → canonical writer → semantic snapshots | Against Oracle-bound cases **not** listed as excluded below |
| Oracle-bound model rows | Algebra/property rows already sealed in `compat/model/v2.5.json` | e.g. `M1-MD-010`…`014`, `017`, `019` and their fixtures |
| Tracefile grammar / coverage-model planning | Use as normative specs | Implementation must follow docs; does not clear blocked_case_ids |

## Explicitly excluded from v0.1 (deferred)

These remain live inventory / contract residuals. They are **not** closed, not
marked reviewed-without-suite, and not product-compatible.

### A. Behavior primary signed N/A (7)

Owner: residual program (closed as signed N/A).  
Deferral milestone: post-v0.1 inventory/applicability or toolchain/normalizer program.  
Evidence: `reviews/m0-residual-s5-signed-na.md`.

| Target | Why excluded from v0.1 activation |
| --- | --- |
| `command.geninfo.option.compat-libtool` | No exact-v1 exit/filesystem delta on pinned Oracle |
| `command.lcov.option.compat-libtool` | Same capture path no-op |
| `command.perl2lcov.option.preserve` | Only unstable parallel temp dirs; no approved normalizer |
| `lcovrc.rtl-file-extensions` | Public tools never call `is_language('rtl')` |
| `lcovrc.geninfo-compat-libtool` | Same `.libs` strip no-op class |
| `lcovrc.geninfo-gcov-all-blocks` | gcc-12 intermediate path; classic `-a` ineffective |
| `lcovrc.geninfo-interval-update` | stdout/profile only; not exact-v1 stable |

**Activation rule:** `validate.py --mode m0-ready` may still fail on these seven.
v0.1 authorization uses this matrix + go/no-go instead of requiring zero gaps.

### B. Diagnostics Ferricov parity IDs (3)

Owner: product parity after geninfo-child implementation exists.  
Deferral milestone: after M1 capture/parallel work (not CORE-001…008).  
Evidence: wave3 bound Oracle twins; FERRICOV IDs intentionally unbound.

| ID | Why excluded |
| --- | --- |
| `PAR-GENINFO-CHILD-EXIT-FERRICOV-001` | Needs Ferricov product child-exit parity, not Oracle-only seal |
| `PAR-GENINFO-CHILD-IGNORE1-FERRICOV-001` | Same |
| `PAR-GENINFO-CHILD-IGNORE2-FERRICOV-001` | Same |

### C. Model decision blockers (3)

Owner: M1-CORE-009+ / product limit selection.  
Evidence: `reviews/m0-model-blocker-scope.md`.  
Live contract: remain in `compat/model/v2.5.json` `blocked_case_ids`.

| ID | Why excluded from v0.1 core |
| --- | --- |
| `M1-MD-020` | Adversarial fuzz execution remains M1-only; not required for CORE-001…008 |
| `M1-TF-063` | Ferricov product resource-limit / over-limit parity; no product limits yet |
| `M1-TF-064` | Executable fuzz corpus + CI budgets; deferred with CORE-009 |

### D. Product evidence gate

| Gate | v0.1 rule |
| --- | --- |
| `product_compatibility_evidence` | Must stay **false** on all domain contracts until CORE-010 produces Ferricov-vs-Oracle evidence for an applicable case set |
| Compatibility marketing / drop-in claims | Forbidden in v0.1 |

## Task gating under this matrix

| Task | Authorized under this matrix? |
| --- | --- |
| `M1-CORE-001` … `M1-CORE-008` | **YES** |
| `M1-CORE-009` (properties/fuzz) | **NO** until `M1-MD-020` / `M1-TF-064` resolved or matrix revised |
| `M1-CORE-010` (differential closure) | **YES to begin**, but cannot set product evidence true until case-by-case parity review; excluded IDs above stay out |
| `M1-CORE-011` (performance) | **NO** until after CORE-010 parity for the in-scope set |

## Non-negotiables

1. Do not hollow-close the 7 signed-N/A primaries.
2. Do not bind the 3 `*-FERRICOV-001` IDs with Oracle-only observations.
3. Do not remove `M1-MD-020` / `M1-TF-063` / `M1-TF-064` from `blocked_case_ids` without executable evidence.
4. Do not copy Perl internal structure or weaken Oracle differential identity rules.
5. Do not widen ownership into CLI / lcovrc / report / capture without a new matrix revision.

## Revision rule

Any change to in-scope tasks, exclusions, or product-evidence policy requires:

1. edit this file,
2. a new go/no-go revision (or explicit amendment section),
3. regenerated `docs/ssot/m0-status.snapshot.json`,
4. controller Critical review.

## Controller signature

Support matrix approved for **conditional M1 Tracefile Core activation** of
`M1-CORE-001`…`M1-CORE-008` only, with exclusions A–D above.

