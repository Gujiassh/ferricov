# Benchmarks

Benchmarks compare Ferricov with the pinned LCOV 2.5 Oracle only after the
fixture passes differential correctness checks. Release evidence records raw
samples and the complete environment; uncommitted exploratory output belongs
under `bench/results/local/`.
